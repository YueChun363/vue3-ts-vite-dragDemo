enum editorTypeEnum {
    left = 'left',// 左边
    right = 'right',// 右边
    bottom = 'bottom',// 下边
    top = 'top',// 上边
    leftTop = 'left-top',// 左上
    leftBottom = 'leftBottom', // 左下
    rightBottom = 'right-bottom',// 右下
    rightTop = 'right-top',// 右上 
}

export {
    editorTypeEnum,
}